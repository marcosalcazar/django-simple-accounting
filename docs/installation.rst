============
Installation
============

At the command line::

    $ easy_install django-simple-accounting

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-simple-accounting
    $ pip install django-simple-accounting
